#!/bin/sh


# Load common.igni with common tasks:
. /Shared/Igniters/common.igni


# Initialize service:
ignite \
    name="Softwarebundlename" \
    action="${1:-status}" \
    start_cmd="'echo \"yea this is cool!\"'" \
    test_cmd="'{{ unbound_softwaredir }}/exports/unbound-checkconf'" \
    reload_cmd="'/Shared/Igniters/nginx.igni reload'" \
    test_cmd="'./my-magic-shiny-test-framework my arguments'"
